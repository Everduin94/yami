image: node:10

build:
  stage: build
  cache:
    paths:
      - node_modules/
  script:
    - npm install --quiet
    - npm run build
  artifacts:
    paths:
      - dist/yami/

deploy_prod:
  stage: deploy
  environment: 
    name: production
    url: $FIREBASE_URL
  only: 
   - master
  dependencies:
    - build
  cache:
    policy: pull
    paths:
      - node_modules/
  script:
    - npm install -g firebase-tools
    - npm run deploy

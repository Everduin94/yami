import { PrototypeDirective } from './prototype.directive';

describe('PrototypeDirective', () => {
  it('should create an instance', () => {
    const directive = new PrototypeDirective();
    expect(directive).toBeTruthy();
  });
});

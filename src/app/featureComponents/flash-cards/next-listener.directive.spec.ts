import { NextListenerDirective } from './next-listener.directive';

describe('NextListenerDirective', () => {
  it('should create an instance', () => {
    const directive = new NextListenerDirective();
    expect(directive).toBeTruthy();
  });
});

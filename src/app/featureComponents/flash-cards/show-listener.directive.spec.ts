import { ShowListenerDirective } from './show-listener.directive';

describe('ShowListenerDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowListenerDirective();
    expect(directive).toBeTruthy();
  });
});
